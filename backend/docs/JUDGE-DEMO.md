# RecoverAI — Judge Demo Script

## 90-second proof

1. Open **Overview** and point to **Provider-verified recovery**.
2. Open **Incidents** and click one incident.
3. Show **Detect → decide → act → verify** timeline.
4. Open **Policy & guardrails** and point to:
   - max retries
   - cooldown
   - human approval threshold
   - chargeback stop
   - abandonment window
5. Open **Proof Lab** and run the 50-event sandbox batch.
6. Say the numbers exactly as labelled:
   - provider-verified recovery = real provider-backed evidence
   - 50-event run = sandbox simulation
7. Open **Recovery links** and show the Payment Link mechanism.
8. Open **Architecture** and show the end-to-end control plane.

## Claims to avoid

Do not call a Razorpay Test Mode transaction real money. Razorpay explicitly describes Test Mode as simulated and says no real money is used. Do not claim a WhatsApp message was delivered unless a real WhatsApp provider was integrated; the demo records it as `queued`.
